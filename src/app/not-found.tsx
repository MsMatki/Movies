import React, { FC } from 'react'

const NotFound: FC = () => {
  return (
    <main className="not-found output-message">
        <h1>Not Found</h1>
        <p>Unfortunately, we could not find the requested page or resource.</p>
    </main>
  )
}

export default NotFound